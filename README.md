# Expense CLI

A simple command-line expense tracker.

## Usage
```bash
python expense.py add 250 food "lunch"
python expense.py add 1200 transport
python expense.py list
python expense.py total
python expense.py summary
```

## Data
Stored in `expenses.json` (gitignored).
